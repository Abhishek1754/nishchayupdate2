class EPayGateway:

    @classmethod
    def recharge(
        cls,
        provider_code,
        mobile_number,
        amount,
        transaction_id
    ):

        raise NotImplementedError(
            "EPay integration pending."
        )